# Configuração do basename para roteamento
# Para desenvolvimento local (sem subdiretório)
VITE_BASENAME=/
VITE_API_BASE_URL=/api

# Para produção (com subdiretório /ui-ct-one)
# VITE_BASENAME=/ui-ct-one
# VITE_API_BASE_URL=/ui-ct-one/api
